version: '3.8'

services:
  secure-messenger:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Uncomment the following lines if you want to persist data
    # volumes:
    #   - ./data:/app/data